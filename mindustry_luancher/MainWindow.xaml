<Window x:Class="MindustryLauncherGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MDL - Mindustry Launcher" Height="740" Width="1044"
        WindowStartupLocation="CenterScreen" 
        Background="Transparent" WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResize"
        FontFamily="/Fonts/#fontello, Microsoft YaHei, Segoe UI"
        TextOptions.TextFormattingMode="Ideal" 
        UseLayoutRounding="True" 
        SnapsToDevicePixels="True"
        Loaded="Window_Loaded" Closed="Window_Closed">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="15" CornerRadius="0" GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="NoFocusStyle" TargetType="Control">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <Style x:Key="WindowControlBtnStyle" TargetType="Button" BasedOn="{StaticResource NoFocusStyle}">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1A000000"/>
                                <Setter Property="Foreground" Value="#222"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCloseBtnStyle" TargetType="Button" BasedOn="{StaticResource NoFocusStyle}">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlayBackBtnStyle" TargetType="Button" BasedOn="{StaticResource NoFocusStyle}">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8,0,0,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6,30,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5"/>
                            <ToggleButton x:Name="toggleButton" Background="Transparent" BorderThickness="0" Focusable="False" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <TextBlock Text="▼" Foreground="#888" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" FontSize="10" RenderTransformOrigin="0.5,0.5">
                                                <TextBlock.RenderTransform>
                                                    <RotateTransform x:Name="arrowTransform" Angle="0"/>
                                                </TextBlock.RenderTransform>
                                            </TextBlock>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="arrowTransform" Storyboard.TargetProperty="Angle" To="180" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetName="arrowTransform" Storyboard.TargetProperty="Angle" To="0" Duration="0:0:0.2"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" Margin="{TemplateBinding Padding}" IsHitTestVisible="False" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                            <TextBox x:Name="PART_EditableTextBox" Margin="{TemplateBinding Padding}" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" HorizontalAlignment="Stretch" Visibility="Collapsed" IsReadOnly="{TemplateBinding IsReadOnly}" Text="{Binding Text, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                            <Popup x:Name="PART_Popup" AllowsTransparency="True" PlacementTarget="{Binding ElementName=border}" Placement="Bottom" Focusable="False">
                                <Border x:Name="dropDownBorder" Background="White" BorderBrush="#CCC" BorderThickness="1" CornerRadius="5" Margin="0,3,0,15" Width="{Binding ActualWidth, ElementName=border}" MaxHeight="250" Opacity="0" RenderTransformOrigin="0.5,0">
                                    <Border.RenderTransform>
                                        <ScaleTransform x:Name="dropDownScale" ScaleY="0.8"/>
                                    </Border.RenderTransform>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" Opacity="0.15" ShadowDepth="3" Direction="270"/>
                                    </Border.Effect>
                                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <BooleanAnimationUsingKeyFrames Storyboard.TargetName="PART_Popup" Storyboard.TargetProperty="IsOpen">
                                                <DiscreteBooleanKeyFrame KeyTime="0" Value="True"/>
                                            </BooleanAnimationUsingKeyFrames>
                                            <DoubleAnimation Storyboard.TargetName="dropDownBorder" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="dropDownScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="dropDownBorder" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.15"/>
                                            <DoubleAnimation Storyboard.TargetName="dropDownScale" Storyboard.TargetProperty="ScaleY" To="0.8" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseIn"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <BooleanAnimationUsingKeyFrames Storyboard.TargetName="PART_Popup" Storyboard.TargetProperty="IsOpen">
                                                <DiscreteBooleanKeyFrame KeyTime="0:0:0.15" Value="False"/>
                                            </BooleanAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="3" Margin="4,2">
                            <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F0F4F8"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E3F2FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource NoFocusStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#10000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SidebarButtonStyle" TargetType="Button" BasedOn="{StaticResource NoFocusStyle}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="0,5,5,0">
                            <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0A000000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1A000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LeftSubTabButtonStyle" TargetType="RadioButton" BasedOn="{StaticResource NoFocusStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="Transparent" Padding="15,12" Margin="0,0,0,5" CornerRadius="5" Cursor="Hand">
                            <TextBlock x:Name="txt" Text="{TemplateBinding Content}" Foreground="#555" FontWeight="Bold" HorizontalAlignment="Left"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#E3F2FD" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="border" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="Transparent" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                                <Setter TargetName="txt" Property="Foreground" Value="#2196F3"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="IsChecked" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#F5F5F5"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg">
                            <Border Background="#12000000" CornerRadius="6" Margin="2,0,0,0"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="true">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#35000000" CornerRadius="6" Margin="2,0,0,0"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel"/>
            <Setter Property="VirtualizingPanel.CacheLengthUnit" Value="Item"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Border Background="White" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Opacity="0.15" ShadowDepth="0" Direction="0"/>
            </Border.Effect>
        </Border>

        <Border Background="#F3F4F7" CornerRadius="8" BorderBrush="#DDD" BorderThickness="1"
                RenderOptions.ClearTypeHint="Enabled" TextOptions.TextRenderingMode="ClearType">

            <Grid x:Name="MainContainer">
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="White" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <TextBlock Text="MDL" Foreground="#2196F3" FontSize="26" FontWeight="Bold" VerticalAlignment="Center" Margin="20,0,40,0"/>

                            <Button x:Name="NavLaunchBtn" Style="{StaticResource NavButtonStyle}" Click="NavLaunch_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M 6,4 L 22,12 L 6,20 Z" Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" Width="14" Height="14" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                    <TextBlock Text="启动" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavDownloadBtn" Style="{StaticResource NavButtonStyle}" Click="NavDownload_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M 12,2 L 12,16 M 7,11 L 12,17 L 17,11 M 4,21 L 20,21" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" StrokeLineJoin="Round" Fill="Transparent" Width="14" Height="14" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                    <TextBlock Text="下载" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavModBrowserBtn" Style="{StaticResource NavButtonStyle}" Click="NavModBrowser_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M 12,2 L 22,7 L 22,17 L 12,22 L 2,17 L 2,7 Z M 12,2 L 12,12 L 22,7 M 12,12 L 2,7 M 12,12 L 12,22" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2" StrokeLineJoin="Round" Fill="Transparent" Width="15" Height="15" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                    <TextBlock Text="模组" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavSchematicsBtn" Style="{StaticResource NavButtonStyle}" Click="NavSchematics_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M 9,3 L 3,6 L 3,21 L 9,18 L 15,21 L 21,18 L 21,3 L 15,6 L 9,3 Z M 9,3 L 9,18 M 15,6 L 15,21" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2" StrokeLineJoin="Round" Fill="Transparent" Width="15" Height="15" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                    <TextBlock Text="蓝图" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="NavMultiplayerBtn" Style="{StaticResource NavButtonStyle}" Click="NavMultiplayer_Click">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌐" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="联机" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button x:Name="NavSettingsBtn" Style="{StaticResource NavButtonStyle}" Click="NavSettings_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M 12,15 A 3,3 0 1 0 12,9 A 3,3 0 1 0 12,15 M 10.5,2 L 13.5,2 L 14,5.5 A 7,7 0 0 1 17,7 L 20,5.5 L 21.5,8 L 19,10.5 A 7,7 0 0 1 19,13.5 L 21.5,16 L 20,18.5 L 17,17 A 7,7 0 0 1 14,18.5 L 13.5,22 L 10.5,22 L 10,18.5 A 7,7 0 0 1 7,17 L 4,18.5 L 2.5,16 L 5,13.5 A 7,7 0 0 1 5,10.5 L 2.5,8 L 4,5.5 L 7,7 A 7,7 0 0 1 10,5.5 Z" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2" StrokeLineJoin="Round" Fill="Transparent" Width="15" Height="15" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                    <TextBlock Text="设置" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                            <Button x:Name="NavMoreBtn" Style="{StaticResource NavButtonStyle}" Click="NavMore_Click">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="M 3,6 L 21,6 M 3,12 L 21,12 M 3,18 L 21,18" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="2" StrokeLineJoin="Round" StrokeEndLineCap="Round" Fill="Transparent" Width="15" Height="15" Margin="0,0,8,0" Stretch="Uniform" VerticalAlignment="Center"/>
                                    <TextBlock Text="更多" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>
                

                <TabControl x:Name="MainTabControl" Grid.Row="1" BorderThickness="0" Background="Transparent" SelectionChanged="MainTabControl_SelectionChanged">
                    <TabControl.Template>
                        <ControlTemplate TargetType="TabControl">
                            <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent">
                                <ContentPresenter.Resources>
                                    <Storyboard x:Key="FadeIn">
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="10" To="0" Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <CircleEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </ContentPresenter.Resources>
                                <ContentPresenter.RenderTransform>
                                    <TranslateTransform Y="0"/>
                                </ContentPresenter.RenderTransform>
                            </ContentPresenter>
                        </ControlTemplate>
                    </TabControl.Template>

                    <TabItem>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="280"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="White" BorderBrush="#EEE" BorderThickness="0,0,1,0" CornerRadius="0,0,0,8">
                                <StackPanel Margin="20" VerticalAlignment="Center">
                                    <Image x:Name="MainGameIcon" Width="80" Height="80" Margin="0,0,0,15"/>
                                    <TextBlock Text="Mindustry" FontSize="18" HorizontalAlignment="Center" Margin="0,0,0,30" Foreground="#555"/>
                                    <Button x:Name="LaunchBtn" Height="65" Background="#2196F3" Foreground="White" BorderThickness="0" Click="LaunchGame_Click" Cursor="Hand" FocusVisualStyle="{x:Null}">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                        <StackPanel>
                                            <TextBlock Text="启动游戏" FontSize="20" HorizontalAlignment="Center"/>
                                            <TextBlock x:Name="CurrentLaunchVersionText" Text="未选择版本" FontSize="12" Foreground="#DDFFFFFF" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Button>
                                    <Grid Margin="0,10,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Button Grid.Column="0" Content="版本选择" Height="40" Background="White" BorderBrush="#CCC" Click="OpenVersionSelect_Click" FocusVisualStyle="{x:Null}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="3"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                        <Button Grid.Column="2" Content="版本设置" Height="40" Background="White" BorderBrush="#CCC" Click="OpenVersionSettings_Click" FocusVisualStyle="{x:Null}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="3"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            <Grid Grid.Column="1" Margin="20">
                                <Border Background="#E3F2FD" CornerRadius="5" Padding="15" VerticalAlignment="Top">
                                    <TextBlock Text="📰 欢迎使用 MDL，享受极致丝滑的 Mindustry 启动体验。" Foreground="#1976D2" FontSize="14"/>
                                </Border>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem>
                        <Grid Margin="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" BorderBrush="#EEE" BorderThickness="0,0,1,0" Margin="0,0,20,0">
                                <StackPanel>
                                    <TextBlock Text="下载源" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="5,0,0,20"/>
                                    <RadioButton x:Name="RbOfficial" Content="官方原版" Style="{StaticResource LeftSubTabButtonStyle}" Tag="Anuken/Mindustry" Checked="DownloadSource_Checked"/>
                                    <RadioButton Content="X端 (TinyLake)" Style="{StaticResource LeftSubTabButtonStyle}" Tag="TinyLake/MindustryX" Checked="DownloadSource_Checked"/>
                                    <RadioButton Content="Foo端 (Antigrief)" Style="{StaticResource LeftSubTabButtonStyle}" Tag="mindustry-antigrief/mindustry-client-v8-builds" Checked="DownloadSource_Checked"/>
                                </StackPanel>
                            </Border>
                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="DownloadSourceTitle" Text="获取新版本" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                                <TextBlock x:Name="RemoteVersionLoadingText" Grid.Row="1" Text="正在从 GitHub 拉取版本列表..." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="16"/>
                                <ListBox x:Name="RemoteVersionListBox" Grid.Row="1" Background="White" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Visibility="Collapsed" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="Transparent" BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="15" Margin="5" ToolTip="右键查看更新说明" MouseRightButtonUp="RemoteVersion_RightClick">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding TagName}" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                                                        <TextBlock Text="可下载的发行版本 (右键查看日志)" Foreground="Gray" FontSize="12" Margin="0,5,0,0"/>
                                                    </StackPanel>
                                                    <Button Grid.Column="1" Content="下载" Width="80" Height="35" Background="#2196F3" Foreground="White" BorderThickness="0" Click="DownloadVersion_Click" Tag="{Binding}" Cursor="Hand" FocusVisualStyle="{x:Null}">
                                                        <Button.Resources>
                                                            <Style TargetType="Border">
                                                                <Setter Property="CornerRadius" Value="5"/>
                                                            </Style>
                                                        </Button.Resources>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <Border x:Name="DownloadPanel" Grid.Row="1" VerticalAlignment="Bottom" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="5" Padding="15" Visibility="Collapsed" Margin="20">
                                    <StackPanel>
                                        <TextBlock x:Name="StatusText" Text="准备下载..." FontWeight="Bold" Margin="0,0,0,5"/>
                                        <ProgressBar x:Name="DownloadProgressBar" Height="15" Minimum="0" Maximum="100"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem>
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <DockPanel Grid.Row="0" Margin="0,0,0,15">
                                <TextBlock Text="模组浏览器" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                                <Button Content="刷新列表" DockPanel.Dock="Right" Padding="15,5" Margin="10,0,0,0" Background="White" BorderBrush="#CCC" Click="RefreshModBrowser_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="3" DockPanel.Dock="Right" HorizontalAlignment="Right" Background="White">
                                    <TextBox x:Name="ModSearchBox" Width="250" VerticalContentAlignment="Center" Padding="5" BorderThickness="0" Background="Transparent" TextChanged="ModSearchBox_TextChanged"/>
                                </Border>
                                <TextBlock Text="🔍 搜索:" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray"/>
                            </DockPanel>
                            <TextBlock x:Name="ModBrowserLoadingText" Grid.Row="1" Text="正在从 GitHub 拉取官方模组列表..." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="16"/>
                            <ListBox x:Name="ModBrowserListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Visibility="Collapsed" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="White" CornerRadius="5" Margin="5" Padding="15" ToolTip="右键查看详情" MouseRightButtonUp="ModItem_RightClick">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="100"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Width="48" Height="48" CornerRadius="5" Margin="0,15,15,0" VerticalAlignment="Center" Background="#F5F5F5">
                                                    <Grid>
                                                        <TextBlock Text="🌐" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#CCC"/>
                                                        <Image Source="{Binding IconUrl}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    </Grid>
                                                </Border>
                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <DockPanel>
                                                        <TextBlock Text="{Binding StarsFormatted}" Foreground="#FF9800" FontWeight="Bold" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                                                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="#333"/>
                                                        <TextBlock Text="{Binding Repo}" Foreground="Gray" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                                    </DockPanel>
                                                    <TextBlock Text="{Binding AuthorFormatted}" Foreground="#2196F3" FontSize="12" Margin="0,5,0,5"/>
                                                    <TextBlock Text="右键展示介绍文本" TextWrapping="Wrap" Foreground="#666" MaxHeight="40"/>
                                                </StackPanel>
                                                <Button Grid.Column="2" Content="安装" Height="35" VerticalAlignment="Center" Background="#4CAF50" Foreground="White" BorderThickness="0" Click="InstallModFromBrowser_Click" Tag="{Binding}" Cursor="Hand" FocusVisualStyle="{x:Null}">
                                                    <Button.Resources>
                                                        <Style TargetType="Border">
                                                            <Setter Property="CornerRadius" Value="5"/>
                                                        </Style>
                                                    </Button.Resources>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </TabItem>

                    <TabItem>
                        <Grid Margin="20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="180"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" BorderBrush="#EEE" BorderThickness="0,0,1,0" Margin="0,0,20,0">
                                <StackPanel>
                                    <TextBlock Text="蓝图源" FontSize="18" FontWeight="Bold" Foreground="#333" Margin="5,0,0,20"/>
                                    <RadioButton x:Name="RbSchemMinRi2" Content="MinRi2 档案" Style="{StaticResource LeftSubTabButtonStyle}" Tag="MinRi2/schematics-archives|master" Checked="SchematicSource_Checked"/>
                                    <RadioButton x:Name="RbSchemDesignIt" Content="DesignIt 社区" Style="{StaticResource LeftSubTabButtonStyle}" Tag="MindustryDesignIt/design-it|main" Checked="SchematicSource_Checked"/>
                                </StackPanel>
                            </Border>

                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <DockPanel Grid.Row="0" Margin="0,0,0,15">
                                    <TextBlock x:Name="SchematicSourceTitle" Text="社区蓝图库" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <Button Content="强制重新获取" DockPanel.Dock="Right" Padding="15,5" Margin="10,0,0,0" Background="White" BorderBrush="#CCC" Click="RefreshSchematicBrowser_Click" FocusVisualStyle="{x:Null}">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="3"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                    <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="3" DockPanel.Dock="Right" HorizontalAlignment="Right" Background="White">
                                        <TextBox x:Name="SchematicSearchBox" Width="250" VerticalContentAlignment="Center" Padding="5" BorderThickness="0" Background="Transparent" TextChanged="SchematicSearchBox_TextChanged"/>
                                    </Border>
                                    <TextBlock Text="🔍 搜索:" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray"/>
                                </DockPanel>

                                <TextBlock x:Name="SchematicBrowserLoadingText" Grid.Row="1" Text="正在下载并解析仓库 ZIP，首次拉取可能需要十几秒，请耐心等待..." HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" FontSize="16"/>

                                <ListBox x:Name="SchematicBrowserListBox" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Visibility="Collapsed" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="White" CornerRadius="5" Margin="5" Padding="15">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="100"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel VerticalAlignment="Center">
                                                        <DockPanel>
                                                            <TextBlock Text="📐" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                                            <TextBlock Text="{Binding UI_Name}" FontSize="16" FontWeight="Bold" Foreground="#333" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                        </DockPanel>
                                                        <TextBlock Text="{Binding UI_Description}" Foreground="#666" FontSize="12" Margin="30,5,0,0" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                    <Button Grid.Column="1" Content="下载" Height="35" VerticalAlignment="Center" Background="#2196F3" Foreground="White" BorderThickness="0" Click="InstallSchematicFromBrowser_Click" Tag="{Binding}" Cursor="Hand" FocusVisualStyle="{x:Null}">
                                                        <Button.Resources>
                                                            <Style TargetType="Border">
                                                                <Setter Property="CornerRadius" Value="5"/>
                                                            </Style>
                                                        </Button.Resources>
                                                    </Button>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </Grid>
                    </TabItem>
                    <TabItem>
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="联机大厅 (EasyTier 虚拟局域网)" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                            <Border Grid.Row="1" Background="White" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Padding="20" VerticalAlignment="Top">
                                <StackPanel>
                                    <Border Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="组件状态：" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                                            <TextBlock x:Name="EasyTierStatusText" Text="请先点击下方下载组件..." FontSize="14" FontWeight="Bold" Foreground="#2196F3"/>
                                            <ProgressBar x:Name="EasyTierProgressBar" Height="10" Minimum="0" Maximum="100" Margin="0,10,0,0" Visibility="Collapsed"/>
                                            <Button x:Name="BtnDownloadEasyTier" Content="⏬ 下载 / 修复联机组件" Margin="0,10,0,0" Padding="10,5" Background="#E3F2FD" Foreground="#1976D2" BorderThickness="0" Cursor="Hand" Click="BtnDownloadEasyTier_Click" HorizontalAlignment="Left" FocusVisualStyle="{x:Null}">
                                                <Button.Resources>
                                                    <Style TargetType="Border">
                                                        <Setter Property="CornerRadius" Value="3"/>
                                                    </Style>
                                                </Button.Resources>
                                            </Button>
                                        </StackPanel>
                                    </Border>

                                    <TextBlock Text="1. 公共服务器节点 (保持默认即可):" Margin="0,0,0,5" FontWeight="Bold"/>
                                    <TextBox x:Name="EasyTierServerBox" Text="tcp://101.133.167.126:11010" Padding="8" Margin="0,0,0,15" Background="#F5F5F5" BorderBrush="#CCC" BorderThickness="1" Foreground="Gray"/>

                                    <TextBlock Text="2. 联机房间号 (6位数字):" Margin="0,0,0,5" FontWeight="Bold"/>
                                    <DockPanel Margin="0,0,0,20">
                                        <Button x:Name="GenerateRoomBtn" Content="🎲 随机生成房间" DockPanel.Dock="Right" Margin="10,0,0,0" Padding="15,0" Background="#FFF9C4" Foreground="#F57F17" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Click="GenerateRoom_Click" FocusVisualStyle="{x:Null}">
                                            <Button.Resources>
                                                <Style TargetType="Border">
                                                    <Setter Property="CornerRadius" Value="3"/>
                                                </Style>
                                            </Button.Resources>
                                        </Button>
                                        <TextBox x:Name="EasyTierRoomBox" Text="" Padding="8" Background="#FFF" BorderBrush="#2196F3" BorderThickness="2" FontSize="18" FontWeight="Bold" HorizontalContentAlignment="Center"/>
                                    </DockPanel>

                                    <Button x:Name="ToggleEasyTierBtn" Content="🚀 提权并连接虚拟局域网" FontSize="16" FontWeight="Bold" Padding="15" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand" Click="ToggleEasyTier_Click" FocusVisualStyle="{x:Null}">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>

                                    <TextBlock Text="* 提示：建立虚拟网卡需要请求管理员权限（UAC）。连接成功后，Mindustry 的 TCP 和 UDP 将自动通过虚拟网卡接通，无需进行端口映射，在游戏局域网列表可见。" Foreground="Gray" TextWrapping="Wrap" Margin="0,15,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </TabItem>
                    <TabItem>
                        <StackPanel Margin="40">
                            <TextBlock Text="全局设置" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                            <TextBlock Text="下载网络节点 (遇到报错、拉取失败时，请切换节点并重试):" Margin="0,0,0,5" FontWeight="Bold" Foreground="#333"/>
                            <ComboBox x:Name="ProxyNodeBox" Margin="0,0,0,30" SelectionChanged="ProxyNodeBox_SelectionChanged">
                                <ComboBoxItem Content="[极速] 节点 0：官方直连 (Github 原始服务器)"/>
                                <ComboBoxItem Content="[国内推荐] 节点 1：GhFast (ghfast.top)"/>
                                <ComboBoxItem Content="[国内备用] 节点 2：GhProxy (gh-proxy.com)"/>
                                <ComboBoxItem Content="[国内备用] 节点 3：KkGitHub (kkgithub.com)"/>
                                <ComboBoxItem Content="[国内备用] 节点 4：JsDelivr CDN"/>
                            </ComboBox>

                            <TextBlock Text="默认 Java 路径 (未单独设置版本的实例将使用此 Java):" Margin="0,0,0,5" FontWeight="Bold" Foreground="#333"/>
                            <DockPanel Margin="0,0,0,30">
                                <Button Content="浏览..." Width="80" DockPanel.Dock="Right" Margin="10,0,0,0" Click="BrowseGlobalJavaBtn_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button x:Name="ScanGlobalJavaBtn" Content="重新扫描" Width="80" DockPanel.Dock="Right" Background="#E3F2FD" Foreground="#1976D2" BorderThickness="0" Click="AutoScanGlobalJava_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <ComboBox x:Name="GlobalJavaComboBox" IsEditable="True" TextSearch.TextPath="Path" ScrollViewer.CanContentScroll="False">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Padding="2,4" FontSize="13">
                                                <Run Text="{Binding Version}" Foreground="#1976D2" FontWeight="Bold"/>
                                                <Run Text="   "/>
                                                <Run Text="{Binding Path}" Foreground="#555"/>
                                            </TextBlock>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DockPanel>

                            <TextBlock Text="全局 Java 内存分配 (修改后会覆盖默认参数):" Margin="0,0,0,5" FontWeight="Bold" Foreground="#333"/>
                            <DockPanel Margin="0,0,0,30">
                                <CheckBox x:Name="GlobalAutoRamCheck" Content="自动分配 (推荐)" VerticalAlignment="Center" Checked="GlobalAutoRamCheck_Changed" Unchecked="GlobalAutoRamCheck_Changed"/>
                                <TextBlock x:Name="GlobalRamText" Text="4096 MB" Width="90" DockPanel.Dock="Right" TextAlignment="Right" VerticalAlignment="Center" Foreground="#1976D2" FontWeight="Bold"/>
                                <Slider x:Name="GlobalRamSlider" Minimum="1024" Maximum="16384" TickFrequency="512" IsSnapToTickEnabled="True" Value="4096" VerticalAlignment="Center" ValueChanged="GlobalRamSlider_ValueChanged" Margin="15,0,10,0"/>
                            </DockPanel>
                        </StackPanel>
                    </TabItem>

                    <TabItem>
                        <StackPanel Margin="40">
                            <TextBlock Text="更多" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                            <TextBlock Text="社区资料" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>

                            <Button HorizontalAlignment="Left" Padding="20,10" Background="#2196F3" Foreground="White" BorderThickness="0" Cursor="Hand" Click="OpenWiki_Click" FocusVisualStyle="{x:Null}">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🌐" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="访问 MDT Wiki" FontSize="15" VerticalAlignment="Center" FontWeight="Bold"/>
                                </StackPanel>
                            </Button>

                            <TextBlock Text="Mindustry 中文社区百科，获取最详尽的模组教程、新手指南与服务器资料。" Foreground="Gray" Margin="0,10,0,25" TextWrapping="Wrap"/>

                            <Separator Background="#EEE" Margin="0,0,0,25"/>

                            <TextBlock Text="支持与购买" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Button Width="150" Height="40" Margin="0,0,15,0" Background="#FA5C5C" Foreground="White" BorderThickness="0" Cursor="Hand" Click="OpenItch_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                    <Image Source="pack://application:,,,/Assets/itch_logo.png" Height="24" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Button>

                                <Button Width="150" Height="40" Background="#162231" Foreground="White" BorderThickness="0" Cursor="Hand" Click="OpenSteam_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                    <Image Source="pack://application:,,,/Assets/steam_logo.png" Height="24" RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Button>
                            </StackPanel>

                            <TextBlock Text="如果你喜欢 Mindustry，请考虑在官方平台购买以支持独立开发者 Anuken！" Foreground="Gray" TextWrapping="Wrap"/>
                        </StackPanel>
                    </TabItem>
                </TabControl>

                <ResizeGrip Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,3,3" Width="14" Height="14" Cursor="SizeNWSE">
                    <ResizeGrip.Opacity>0.5</ResizeGrip.Opacity>
                </ResizeGrip>

            </Grid>
        </Border>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,12.5,15,0" Panel.ZIndex="1000">
            <Button x:Name="BtnMinimizeOverlay" Style="{StaticResource WindowControlBtnStyle}" Click="BtnMinimize_Click">
                <Path Data="M 0,5 L 10,5" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
            <Button x:Name="BtnCloseOverlay" Style="{StaticResource WindowCloseBtnStyle}" Click="BtnClose_Click">
                <Path Data="M 0,0 L 10,10 M 0,10 L 10,0" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1.5" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </StackPanel>

        <Grid x:Name="VersionSelectOverlay" Background="#F3F4F7" Visibility="Collapsed" Panel.ZIndex="100" Opacity="0" Margin="10">
            <Grid.RenderTransform>
                <TranslateTransform Y="20"/>
            </Grid.RenderTransform>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="#2196F3" Grid.Row="0" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource OverlayBackBtnStyle}" Content="←" Click="CloseOverlays_Click"/>
                    <TextBlock Text="版本选择" Foreground="White" FontSize="18" VerticalAlignment="Center" FontWeight="Bold" Margin="10,0,0,0"/>
                </StackPanel>
            </Border>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Background="White" BorderBrush="#EEE" BorderThickness="0,0,1,0" CornerRadius="0,0,0,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="文件夹列表" Foreground="Gray" Margin="15,15,0,10"/>
                        <ListBox x:Name="FolderListBox" Grid.Row="1" BorderThickness="0" SelectionChanged="FolderListBox_SelectionChanged" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" ToolTip="{Binding}"/>
                                        <Button Grid.Column="1" Content="✖" Foreground="#D32F2F" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="RemoveFolder_Click" Tag="{Binding}" Margin="5,0,0,0" ToolTip="从列表中移除"/>
                                    </Grid>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <Button Grid.Row="2" Content="+ 导入文件夹" Height="40" Background="Transparent" Foreground="#2196F3" Click="AddNewFolder_Click" FocusVisualStyle="{x:Null}"/>
                    </Grid>
                </Border>
                <ListBox x:Name="InstanceListBox" Grid.Column="1" Background="Transparent" BorderThickness="0" Margin="10" SelectionChanged="InstanceListBox_SelectionChanged" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" CornerRadius="5" Margin="5" Padding="15" Cursor="Hand">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding FullPath}" Foreground="Gray" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="删除" Background="#FFEBEE" Foreground="#D32F2F" BorderThickness="0" Padding="15,5" Cursor="Hand" Click="DeleteInstance_Click" Tag="{Binding}" VerticalAlignment="Center">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Grid>

        <Grid x:Name="VersionSettingsOverlay" Background="#F3F4F7" Visibility="Collapsed" Panel.ZIndex="101" Opacity="0" Margin="10">
            <Grid.RenderTransform>
                <TranslateTransform Y="20"/>
            </Grid.RenderTransform>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="#2196F3" Grid.Row="0" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource OverlayBackBtnStyle}" Content="←" Click="CloseOverlays_Click"/>
                    <TextBlock x:Name="VSettingsTitle" Text="版本设置" Foreground="White" FontSize="18" VerticalAlignment="Center" Margin="10,0,0,0" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Background="White" BorderBrush="#EEE" BorderThickness="0,0,1,0" CornerRadius="0,0,0,8">
                    <StackPanel Margin="0,10,0,0">
                        <Button x:Name="VSidebarOverviewBtn" Content="概览" Style="{StaticResource SidebarButtonStyle}" Click="VSidebarOverview_Click"/>
                        <Button x:Name="VSidebarConfigBtn" Content="设置" Style="{StaticResource SidebarButtonStyle}" Click="VSidebarConfig_Click"/>
                        <Button x:Name="VSidebarModBtn" Content="Mod 管理" Style="{StaticResource SidebarButtonStyle}" Click="VSidebarMod_Click"/>
                        <Button x:Name="VSidebarSchematicBtn" Content="蓝图 管理" Style="{StaticResource SidebarButtonStyle}" Click="VSidebarSchematic_Click"/>
                        <Button x:Name="VSidebarSaveDataBtn" Content="存档 管理" Style="{StaticResource SidebarButtonStyle}" Click="VSidebarSaveData_Click"/>
                        <Button x:Name="VSidebarOpenFolderBtn" Content="打开文件夹 ▼" Style="{StaticResource SidebarButtonStyle}" Click="VSidebarOpenFolder_Click">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="打开 本体文件夹" Click="OpenGameFolder_Click"/>
                                    <MenuItem Header="打开 数据文件夹" Click="OpenDataFolder_Click"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                    </StackPanel>
                </Border>
                <Grid Grid.Column="1" Margin="30">
                    <Border x:Name="VSettingsOverviewPanel" Background="White" CornerRadius="5" Padding="20" Visibility="Collapsed">
                        <StackPanel>
                            <TextBlock Text="版本概览" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <Image x:Name="SettingsGameIcon" Width="50" Height="50" Margin="0,0,15,0"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock x:Name="OverviewVersionName" FontSize="18" FontWeight="Bold"/>
                                    <TextBlock Text="Mindustry 独立实例" Foreground="Gray" FontSize="12"/>
                                </StackPanel>
                            </StackPanel>
                            <TextBlock Text="物理路径：" Foreground="Gray" Margin="0,10,0,5"/>
                            <TextBlock x:Name="OverviewVersionPath" TextWrapping="Wrap" Background="#F8F9FA" Padding="10"/>
                            <Button x:Name="StartRenameBtn" Content="修改名称" Width="100" HorizontalAlignment="Left" Click="StartRename_Click" Margin="0,10,0,0" FocusVisualStyle="{x:Null}"/>
                            <StackPanel x:Name="RenamePanel" Orientation="Horizontal" Visibility="Collapsed" Margin="0,10,0,0">
                                <TextBox x:Name="RenameTextBox" Width="200" Padding="5"/>
                                <Button Content="确认" Margin="10,0,0,0" Padding="15,5" Background="#4CAF50" Foreground="White" BorderThickness="0" Click="ConfirmRename_Click" FocusVisualStyle="{x:Null}"/>
                                <Button Content="取消" Margin="5,0,0,0" Padding="15,5" Click="CancelRename_Click" FocusVisualStyle="{x:Null}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <Border x:Name="VSettingsConfigPanel" Background="White" CornerRadius="5" Padding="20">
                        <StackPanel>
                            <TextBlock Text="启动选项" FontSize="16" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,15"/>
                            <TextBlock Text="版本隔离 (开启后存档/Mod独立存放):" Margin="0,0,0,5"/>
                            <ComboBox x:Name="VSettingsIsolationBox" Margin="0,0,0,15">
                                <ComboBoxItem Content="开启 (推荐)"/>
                                <ComboBoxItem Content="关闭"/>
                            </ComboBox>

                            <TextBlock Text="自定义 Java 路径:" Margin="0,0,0,5"/>
                            <DockPanel Margin="0,0,0,15">
                                <Button Content="浏览" Width="60" DockPanel.Dock="Right" Margin="5,0,0,0" Click="BrowseVersionJava_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button x:Name="ScanVersionJavaBtn" Content="重新扫描" Width="70" DockPanel.Dock="Right" Background="#E3F2FD" Foreground="#1976D2" BorderThickness="0" Click="AutoScanVersionJava_Click" FocusVisualStyle="{x:Null}">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <ComboBox x:Name="VSettingsJavaComboBox" IsEditable="True" TextSearch.TextPath="Path" ScrollViewer.CanContentScroll="False">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Padding="2,4" FontSize="13">
                                                <Run Text="{Binding Version}" Foreground="#1976D2" FontWeight="Bold"/>
                                                <Run Text="   "/>
                                                <Run Text="{Binding Path}" Foreground="#555"/>
                                            </TextBlock>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                            </DockPanel>

                            <TextBlock Text="实例 Java 内存分配:" Margin="0,0,0,5"/>
                            <DockPanel Margin="0,0,0,15">
                                <CheckBox x:Name="VersionAutoRamCheck" Content="跟随自动分配" VerticalAlignment="Center" Checked="VersionAutoRamCheck_Changed" Unchecked="VersionAutoRamCheck_Changed"/>
                                <TextBlock x:Name="VSettingsRamText" Text="4096 MB" Width="90" DockPanel.Dock="Right" TextAlignment="Right" VerticalAlignment="Center" Foreground="#1976D2" FontWeight="Bold"/>
                                <Slider x:Name="VSettingsRamSlider" Minimum="1024" Maximum="16384" TickFrequency="512" IsSnapToTickEnabled="True" Value="4096" VerticalAlignment="Center" ValueChanged="VSettingsRamSlider_ValueChanged" Margin="15,0,10,0"/>
                            </DockPanel>

                            <TextBlock Text="自定义 JVM 启动参数 (高级选项):" Margin="0,0,0,5"/>
                            <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="5" Margin="0,0,0,15" Background="White">
                                <TextBox x:Name="VSettingsJvmArgsBox" Padding="8,6" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center" FontSize="13"/>
                            </Border>

                        </StackPanel>
                    </Border>
                    <Grid x:Name="VSettingsModPanel" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel Margin="0,0,0,10">
                            <TextBlock Text="本地 Mod 列表" FontSize="18" FontWeight="Bold"/>
                            <Button Content="刷新" HorizontalAlignment="Right" Click="RefreshMods_Click" Padding="10,5" FocusVisualStyle="{x:Null}"/>
                        </DockPanel>
                        <ListBox x:Name="ModListBox" Grid.Row="1" Background="White" BorderThickness="0" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="10">
                                        <DockPanel>
                                            <Button Content="删除" DockPanel.Dock="Right" Background="#FFEBEE" Foreground="#D32F2F" BorderThickness="0" Click="DeleteMod_Click" Tag="{Binding FullPath}" Padding="15,5" Cursor="Hand" FocusVisualStyle="{x:Null}" VerticalAlignment="Center"/>
                                            <Border Width="48" Height="48" CornerRadius="5" Margin="0,0,15,0" VerticalAlignment="Center" Background="#F5F5F5">
                                                <Grid>
                                                    <TextBlock Text="📦" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#CCC">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IconImage}" Value="{x:Null}">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                    <Image Source="{Binding IconImage}" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                </Grid>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <DockPanel>
                                                    <TextBlock Text="{Binding UI_Name}" FontWeight="Bold" FontSize="15" Foreground="#333" TextTrimming="CharacterEllipsis"/>
                                                    <Border HorizontalAlignment="Left" Background="#E3F2FD" CornerRadius="3" Padding="6,1" Margin="10,0,0,0" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding Version}" FontSize="11" Foreground="#1976D2"/>
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Version}" Value="">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                    </Border>
                                                </DockPanel>
                                                <TextBlock Text="{Binding UI_Author}" Foreground="#888" FontSize="12" Margin="0,3,0,3"/>
                                                <TextBlock Text="{Binding Description}" Foreground="#666" FontSize="12" TextWrapping="Wrap" MaxHeight="35" TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock x:Name="NoModText" Grid.Row="1" Text="此实例暂无 Mod" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" Visibility="Collapsed"/>
                    </Grid>

                    <Grid x:Name="VSettingsSchematicPanel" Visibility="Collapsed">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <DockPanel Margin="0,0,0,10">
                            <TextBlock Text="本地 蓝图 列表" FontSize="18" FontWeight="Bold"/>
                            <Button Content="刷新" HorizontalAlignment="Right" Click="RefreshSchematics_Click" Padding="10,5" FocusVisualStyle="{x:Null}"/>
                        </DockPanel>
                        <ListBox x:Name="LocalSchematicListBox" Grid.Row="1" Background="White" BorderThickness="0" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#EEE" BorderThickness="0,0,0,1" Padding="10">
                                        <DockPanel>
                                            <Button Content="删除" DockPanel.Dock="Right" Background="#FFEBEE" Foreground="#D32F2F" BorderThickness="0" Click="DeleteSchematic_Click" Tag="{Binding FullPath}" Padding="15,5" Cursor="Hand" FocusVisualStyle="{x:Null}" VerticalAlignment="Center"/>
                                            <TextBlock Text="📐" FontSize="20" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding FileName}" FontWeight="Bold" FontSize="15" Foreground="#333" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding FileSize}" Foreground="#888" FontSize="12" Margin="0,3,0,0"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                        <TextBlock x:Name="NoSchematicText" Grid.Row="1" Text="此实例暂无蓝图" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray" Visibility="Collapsed"/>
                    </Grid>

                    <Border x:Name="VSettingsSaveDataPanel" Background="White" CornerRadius="5" Padding="20" Visibility="Collapsed">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="存档管理与急救" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

                            <Border Grid.Row="1" Background="#F8F9FA" BorderBrush="#EEE" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="当前实例索引状态 (settings.bin):" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                                    <TextBlock x:Name="SaveDataStatusText" Text="正在检测..." FontSize="16" FontWeight="Bold" Foreground="#1976D2"/>
                                </StackPanel>
                            </Border>

                            <DockPanel Grid.Row="2" Margin="0,0,0,10">
                                <Button x:Name="ParseSaveDataBtn" Content="🔍 读取编辑" Height="35" Padding="15,0" Background="#E3F2FD" Foreground="#1976D2" BorderThickness="0" Click="ParseSaveData_Click" Cursor="Hand" Margin="0,0,10,0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button x:Name="SaveSettingsBtn" Content="💾 保存修改" Height="35" Padding="15,0" Background="#E8F5E9" Foreground="#2E7D32" BorderThickness="0" Click="SaveSettings_Click" Cursor="Hand" Margin="0,0,10,0">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                                <Button x:Name="RescueSaveDataBtn" Content="🚑 一键重建索引" Height="35" Padding="15,0" Background="#FFEBEE" Foreground="#D32F2F" BorderThickness="0" Click="RescueSaveData_Click" Cursor="Hand">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="3"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <ComboBox x:Name="SettingsCategoryBox" Width="130" Margin="10,0,0,0" SelectionChanged="SettingsCategoryBox_SelectionChanged">
                                    <ComboBoxItem Content="📄 全部数据" IsSelected="True"/>
                                    <ComboBoxItem Content="⚙️ 仅看核心设置"/>
                                    <ComboBoxItem Content="🚀 仅看科技解锁"/>
                                </ComboBox>

                                <Border BorderBrush="#CCC" BorderThickness="1" CornerRadius="3" DockPanel.Dock="Right" HorizontalAlignment="Right" Background="White" Width="180">
                                    <TextBox x:Name="SettingsSearchBox" VerticalContentAlignment="Center" Padding="5" BorderThickness="0" Background="Transparent" TextChanged="SettingsSearchBox_TextChanged"/>
                                </Border>
                                <TextBlock Text="🔍 搜索:" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="Gray" HorizontalAlignment="Right"/>
                            </DockPanel>

                            <DataGrid x:Name="SettingsDataGrid" Grid.Row="3" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" 
                                      HeadersVisibility="Column" Background="White" BorderBrush="#EEE" BorderThickness="1" RowHeight="35"
                                      EnableRowVirtualization="True" EnableColumnVirtualization="True" 
                                      VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling"
                                      VirtualizingPanel.ScrollUnit="Pixel"
                                      PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="键 (Key) - 只读" Binding="{Binding Key}" Width="250" IsReadOnly="True">
                                        <DataGridTextColumn.ElementStyle>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#555"/>
                                                <Setter Property="VerticalAlignment" Value="Center"/>
                                                <Setter Property="Margin" Value="5,0"/>
                                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                            </Style>
                                        </DataGridTextColumn.ElementStyle>
                                    </DataGridTextColumn>
                                    <DataGridTemplateColumn Header="值 (Value) - 可编辑" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBox Text="{Binding DisplayValue, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsBinary}" VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent" Padding="5,0">
                                                    <TextBox.Style>
                                                        <Style TargetType="TextBox">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsBinary}" Value="True">
                                                                    <Setter Property="Foreground" Value="Gray"/>
                                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBox.Style>
                                                </TextBox>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>

                        </Grid>
                    </Border>

                </Grid>
            </Grid>
        </Grid>

        <Grid x:Name="ModInstallOverlay" Background="#99000000" Visibility="Collapsed" Panel.ZIndex="200" Opacity="0">
            <Grid.RenderTransform>
                <TranslateTransform Y="20"/>
            </Grid.RenderTransform>
            <Border Background="White" CornerRadius="8" Width="400" Height="550" VerticalAlignment="Center" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="ModInstallTitle" Text="安装模组" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <StackPanel Grid.Row="1" Margin="0,0,0,15">
                        <TextBlock Text="1. 选择版本:" Margin="0,0,0,5"/>
                        <ComboBox x:Name="ModVersionComboBox" DisplayMemberPath="TagName" SelectionChanged="ModInstallForm_SelectionChanged"/>
                    </StackPanel>
                    <StackPanel Grid.Row="2" Margin="0,0,0,10">
                        <TextBlock Text="2. 选择目标实例:" Margin="0,0,0,5"/>
                        <ListBox x:Name="AllInstancesListBox" Height="250" SelectionChanged="ModInstallForm_SelectionChanged" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="5">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                            <TextBlock Text="{Binding FullPath}" Foreground="Gray" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                    <StackPanel x:Name="ModInstallProgressPanel" Grid.Row="3" Visibility="Collapsed" Margin="0,0,0,15">
                        <TextBlock x:Name="ModInstallStatusText" Text="准备下载..." Margin="0,0,0,5"/>
                        <ProgressBar x:Name="ModInstallProgressBar" Height="10"/>
                    </StackPanel>
                    <DockPanel Grid.Row="4" LastChildFill="False">
                        <Button Content="取消" Width="100" Height="35" Click="CancelModInstall_Click" FocusVisualStyle="{x:Null}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button x:Name="ConfirmModInstallBtn" Content="开始安装" DockPanel.Dock="Right" Width="100" Height="35" Background="#4CAF50" Foreground="White" BorderThickness="0" Click="ConfirmModInstall_Click" IsEnabled="False" FocusVisualStyle="{x:Null}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="SchematicInstallOverlay" Background="#99000000" Visibility="Collapsed" Panel.ZIndex="201" Opacity="0">
            <Grid.RenderTransform>
                <TranslateTransform Y="20"/>
            </Grid.RenderTransform>
            <Border Background="White" CornerRadius="8" Width="400" Height="400" VerticalAlignment="Center" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="SchematicInstallTitle" Text="下载蓝图" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" TextTrimming="CharacterEllipsis"/>
                    <StackPanel Grid.Row="1" Margin="0,0,0,10">
                        <TextBlock Text="选择要安装到的目标实例 (直接从本地缓存解压):" Margin="0,0,0,5"/>
                        <ListBox x:Name="SchematicInstancesListBox" Height="250" SelectionChanged="SchematicInstallForm_SelectionChanged" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="5">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14"/>
                                            <TextBlock Text="{Binding FullPath}" Foreground="Gray" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                    <DockPanel Grid.Row="2" LastChildFill="False">
                        <Button Content="取消" Width="100" Height="35" Click="CancelSchematicInstall_Click" FocusVisualStyle="{x:Null}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                        <Button x:Name="ConfirmSchematicInstallBtn" Content="瞬间安装" DockPanel.Dock="Right" Width="100" Height="35" Background="#4CAF50" Foreground="White" BorderThickness="0" Click="ConfirmSchematicInstall_Click" IsEnabled="False" FocusVisualStyle="{x:Null}">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="ReleaseNotesOverlay" Background="#99000000" Visibility="Collapsed" Panel.ZIndex="300" Opacity="0">
            <Grid.RenderTransform>
                <TranslateTransform Y="20"/>
            </Grid.RenderTransform>
            <Border Background="White" CornerRadius="8" Width="600" Height="450" VerticalAlignment="Center" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="ReleaseNotesTitle" Text="详情" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>
                    <ScrollViewer Grid.Row="1" Margin="0,0,0,15" PreviewMouseWheel="SmoothScroll_PreviewMouseWheel">
                        <TextBlock x:Name="ReleaseNotesText" TextWrapping="Wrap" Foreground="#444" FontSize="14" LineHeight="22"/>
                    </ScrollViewer>
                    <DockPanel Grid.Row="2" LastChildFill="False">
                        <Button x:Name="OpenRepoBtn" Content="前往 GitHub 仓库" Width="150" Height="35" Background="#2196F3" Foreground="White" BorderThickness="0" Click="OpenRepo_Click" FocusVisualStyle="{x:Null}" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button x:Name="ExportCrashBtn" Content="导出报告" Visibility="Collapsed" Width="150" Height="35" Background="#4CAF50" Foreground="White" BorderThickness="0" Click="ExportCrash_Click" FocusVisualStyle="{x:Null}" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <Button Content="关闭" DockPanel.Dock="Right" Width="100" Height="35" Background="#E0E0E0" Foreground="#333" BorderThickness="0" Click="CloseReleaseNotes_Click" FocusVisualStyle="{x:Null}" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</Window>